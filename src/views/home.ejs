<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MERCADO-LIEBRE</title> 
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@100;300&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
    <script defer src="/js/index.js"> </script>
</head>
<body>
    <%- include('./partials/header.ejs') %>
    
    <main class="main">
        <img class="banner" src="/img/img-home-banner.jpg" alt="Banner">
        <!-- ULTIMA VISITA -->
        <section class="ultima-visita">
            <h2 class="title">Basado en tu última visita</h2>

            <% for (let i = 0; i < productsVisited.length; i++) {%> 
            <article class="article">
                <a href="/products/detail/<%= productsVisited[i].id %>">
                    <img src="/img/imgArticulos/<%= productsVisited[i].image %> " alt="<%= productsVisited[i].description %>">
                    <div class="caracteristicas">
                        <i class="fas fa-truck"></i>
                        <% if(productsVisited[i].discount != 0) {%> 
                        <div class="precio-descuento">
                        <p class="precio">$<%= toThousand(productsVisited[i].price) %></p>
                        <p class="descuento"><%= productsVisited[i].discount %> % off</p>
                        </div>
                        <% } else {%>
                            <div class="precio-descuento">
                                <p class="precio">$<%= toThousand(productsVisited[i].price) %></p>
                                <p class="descuento"></p>
                            </div>
                            <% } %> 
                        <p class="descripción"><%= productsVisited[i].name %></p>
                    </div>
                </a>
            </article>
            <% } %> 
        </section>

        <!-- OFERTAS -->
        <section id="ofertas" class="ofertas">
            <h2 class="title">Ofertas</h2>
            <% for (let i = 0; i < productsInSale.length; i++) {%> 
                <article class="article">
                    <a href="/products/detail/<%= productsInSale[i].id %>">
                        <img src="/img/imgArticulos/<%= productsInSale[i].image %> " alt="<%= productsInSale[i].description %>">
                        <div class="caracteristicas">
                            <i class="fas fa-truck"></i>
                            <div class="precio-descuento">
                            <p class="precio">$<%= toThousand(productsInSale[i].price) %></p>
                            <p class="descuento"><%= productsInSale[i].discount %> % off</p>
                            </div>
                            <p class="descripción"><%= productsInSale[i].name %></p>
                        </div>
                    </a>
                </article> 
                <% } %> 
        </section>
    </main>
    
    
<%- include('./partials/footer.ejs') %>

</body>
</html>

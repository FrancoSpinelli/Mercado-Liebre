<!-- <!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('partials/head') %>
<body>
	<!-- Header -->
	<%- include('partials/headerNavbar') %>
	<!-- /Header -->

	<!-- todos los productos -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Todos los productos</h2>
			</div>
		
			<div class="col-12 col-sm-6 col-lg-3">
				<h2>Ultimas visitas</h2>
				<section class="product-box">	
					<% let productosUltimosVisitados = products.filter((product)=> {return product.category === 'visited'}) %> 
					<% for (let i = 0; i < productosUltimosVisitados.length; i++){ %> 
					<a href="/products/<%= productosUltimosVisitados[i].id %>">
						<figure class="product-box_image">
							<img src="/images/products/<%= productosUltimosVisitados[i].image %> " alt="imagen de producto">
						</figure>
						<article class="product-box_data">
							<h2>$<%= toThousand(productosUltimosVisitados[i].price) %></h2>
							<span><%= productosUltimosVisitados[i].discount %>% OFF</span>
							<p><%= productosUltimosVisitados[i].name %></p>
							<i class="fas fa-truck"></i>
						</article>
					</a>
					<% } %> 
				</section>
			</div>

			<div class="col-12 col-sm-6 col-lg-3">
				<h2>Ofertas</h2>
				<section class="product-box">
					<% let productosOfertas = products.filter((product)=> {return product.category === 'in-sale'}) %> 
					<% for (let i = 0; i < productosOfertas.length; i++){ %> 
					<a href="/products/<%= productosOfertas[i].id %>">
						<figure class="product-box_image">
							<img src="/images/products/<%= productosOfertas[i].image %> " alt="imagen de producto">
						</figure>
						<article class="product-box_data">
							<h2>$<%= toThousand(productosOfertas[i].price) %></h2>
							<span><%= productosOfertas[i].discount %>% OFF</span>
							<p><%= productosOfertas[i].name %></p>
							<i class="fas fa-truck"></i>
						</article>
					</a>
					<% } %> 
				</section>
			</div>
		
		</div>
	</div>
	<!-- /todos los productos -->

	<!-- footer -->
	<%- include('partials/footer') %>
	<!-- /footer -->
</body>	
</html> -->